You are an expert EVM dapp starter. Create a minimal Hardhat monorepo for a project named "trustgarden-pixelwar" with:

- Node 20, Hardhat, TypeScript, dotenv, viem, wagmi deps only for typing (no frontend here).
- Hardhat config for EVM-compatible testnet (network name: INTUITION_TESTNET) using env:
  - RPC_URL
  - PRIVATE_KEY
- Install OpenZeppelin contracts.

Produce:
1) contracts/PixelGarden.sol (Solidity 0.8.24) implementing a pixel grid with stake per state:
   - Grid size: configurable (default 64 x 64).
   - States are bytes32 keys (keccak of canonical state names like "empty","soil","seed","sprout","leaf","flower:red","flower:yellow","flower:blue","flower:white","shrub","tree","water","stone","path").
   - TRUST token is an ERC20 (IERC20) at address settable in constructor.
   - struct PixelState { uint256 stake; }
   - struct Pixel { mapping(bytes32 => PixelState) states; bytes32 current; uint64 lastUpdate; }
   - function setPixel(uint32 x, uint32 y, bytes32 state, uint256 amount) external:
        * bounds check x,y
        * transferFrom(msg.sender, address(this), amount) for TRUST
        * increment stake for state
        * recompute current winner: pick state with highest stake (tie-breaker: keep current)
        * update lastUpdate and emit PixelChanged(x,y,oldState,newState, state, amount, msg.sender)
   - view functions:
        * getPixel(uint32 x, uint32 y) returns (bytes32 current, uint256 currentStake)
        * getTopStates(uint32 x, uint32 y, uint8 k) returns arrays of top-k (state, stake) by iterating a fixed list of allowed states
        * stateKey(string name) pure helper to keccak256
   - Admin:
        * allowedStates() returns the canonical list (hard-coded array of strings in contract)
        * constructor(uint32 _w, uint32 _h, address _trustToken)
   - Keep it gas-simple: no coherence/recency/decay yet (we add off-chain later).
   - Events:
        event PixelChanged(uint32 x, uint32 y, bytes32 oldState, bytes32 newState, bytes32 actedState, uint256 amount, address actor);

2) scripts/deploy.ts
   - Deploy PixelGarden with width=64, height=64, TRUST_TOKEN_ADDRESS from env.
   - Log the deployed address.

3) scripts/seed.ts
   - Approve + stake a few pixels for smoke test (if we also deploy a mock TRUST).

4) contracts/mocks/TrustMock.sol (ONLY IF process.env.TRUST_TOKEN_ADDRESS is empty at deploy time)
   - Simple ERC20 mock "TRUST" with 18 decimals, mint to deployer 1e24.

5) README.md with:
   - .env example (RPC_URL, PRIVATE_KEY, TRUST_TOKEN_ADDRESS optional)
   - Commands:
      - npm i
      - npx hardhat compile
      - npx hardhat run scripts/deploy.ts --network INTUITION_TESTNET
      - (optional) npx hardhat run scripts/seed.ts --network INTUITION_TESTNET

6) Hardhat tasks:
   - task("set:pixel", ...) to call setPixel(x,y,stateName,amount) where stateName is string converted to bytes32 in TS (hash in script), read from CLI params.
   - task("pixel:get", ...) logs current + top3.

TypeScript utility:
- src/utils/states.ts: export the canonical ordered list of state names and a helper to hash (same as contract order). Ensure the contract and TS share EXACT same list.

IMPORTANT:
- Enforce allowance/approve requirement in README (user must approve TRUST to contract before setPixel).
- Keep code clean, documented, and minimal. No frontend in this repo.
